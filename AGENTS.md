# AGENTS.md — プロジェクト開発・運用憲法 兼 開発ガイド（マスターファイル）

本ドキュメントは、本リポジトリで活動するすべてのAIエージェント（指揮官Antigravity、実行部隊Jules、その他エージェント）および人間に適用される**プロジェクト唯一の総合規約・マスターガイド**です。

---

## 🎯 1. プロジェクト概要 & 技術スタック
- **プロジェクト名**: NDLOCR-Lite カスタマイズ OCR アプリ（範囲指定OCR）
- **成果物**: NDLOCR-Lite（国立国会図書館 / CC BY 4.0）をベースとし、**マウスで矩形範囲を選択してその範囲だけOCRする機能**を追加したデスクトップアプリケーション
- **カスタマイズ方針（C方式: fork ＋ 追加モジュール）**: 上流コードは原則不可侵とし、**新規ディレクトリ `custom_gui/` および `tests/` にのみ実装**する。**モデルの再学習（`train/` 配下）は本プロジェクトのスコープ外**とする。
- **ベースソフトウェア**: [ndl-lab/ndlocr-lite](https://github.com/ndl-lab/ndlocr-lite) v1.2.3 (2026-06-09) / CC BY 4.0
- **リポジトリ**: `a19156664-bot/ndlocr-lite-custom`（fork元: `ndl-lab/ndlocr-lite`）
- **統合先ブランチ**: **`master`**（fork由来のデフォルトブランチ。本プロジェクトでは `main` ではなく `master` が保護対象）
- **検証環境**: Python 3.13.5（ローカル） / Python 3.10 以上 / デスクトップGUI（flet）
- **テスト検証コマンド**: `python -m pytest tests/ -v`

### 1.1 上流（upstream）との関係
- `origin` = `a19156664-bot/ndlocr-lite-custom`（自リポジトリ・push可）
- `upstream` = `ndl-lab/ndlocr-lite`（**fetch専用。push URLは無効化済み**）
- **国立国会図書館のリポジトリへの push・PR送信は、いかなる場合も行ってはならない。**

---

## 👑 2. 厳格な役割境界（最重要絶対ルール）

本プロジェクトにおいて、エージェントは以下の役割境界を**絶対に遵守**しなければなりません。

| エージェント | 役割 | 専任業務 | 絶対禁止事項 |
|---|---|---|---|
| **Antigravity** | 指揮官・アーキテクト・工程管理者 | 要件定義、DoD策定、Julesへの指示作成、PR/パッチ静的検収、テスト検証、マージ判定、日報更新 | **アプリケーション機能コード（JS/HTML/CSS/Test等）の直接編集・自ら実装・コード生成することの完全禁止** |
| **Jules** | 実行部隊・プログラム生成AI | 指示書に基づく機能プログラム生成、新規単体テスト記述、コード修正、PR/パッチ送信 | 指示範囲を逸脱したリファクタリング、既存テストの破壊・変更 |

---

## ⚡ 3. 標準運用モード: パターンB（パイプライン一括検収・まとめ承認モデル）

開発スピードと効率を最大化するため、**パターンB（一括パイプラインモデル）**を標準運用フローとします。

1. **Phase 1: 自律連続発注 (Antigravity)**
   - Antigravity は `.nightly/ROADMAP.md` に定義されたタスク群を、人間の個別の承認を挟まずに Jules CLI 経由で連続して自動発注・セッション作成する。
2. **Phase 2: コード生成 & メッセージ自動検収 (Jules & Antigravity)**
   - Jules が各タスクのプログラム、単体テスト、セッションメッセージを生成。
   - Antigravity は Jules CLI / API 経由で Jules からのログ・質問・完了確認メッセージ (`Needs clarification`) を自律監視・取得する。
   - Jules が完了確認を求めている場合は、Antigravity が自動的に完了承認レスポンス (`Wrap it up / Approve`) を返答し、セッションを完了状態 (`Completed`) へ遷移させる。
   - 各タスク完了ごとに自律して成果物を「使い捨て統合ブランチ」へ適用し、単体テスト (`python -m pytest tests/ -v`) 実行および **`git diff` によるコード意味の実質性監査（静的レビュー）** を並置して品質をチェック・積載する。
3. **Phase 3: 最終一括検収 & 人間一括承認 (Antigravity & 人間)**
   - 全タスク（または指定ブロック）が完了し、単体テストおよび静的コード監査が合格していることを確認した上で、Antigravity が**総括検収レポート**を作成して人間に提示する。
   - 人間の明示的一括承認 (`[WRITE-I]`) を得て、使い捨て統合ブランチから一括で `master` ブランチへマージ・プッシュする。（承認前の段階であれば統合ブランチごと破棄可能であり、完全な可逆性が保証される）。

### 3.1 使い捨て統合ブランチの命名規則
統合ブランチ名は **`integration/task-<開始ID>-<終了ID>`** 形式とする（例: `integration/task-28-31`、単独タスクは `integration/task-32`）。

- **目的**: ブランチ名だけで「破棄した場合に何が失われるか」が即座に判明し、人間の破棄判断を最速化するため。
- 日付ベース（`integration/nightly-YYYYMMDD`）は、1日に複数ブロックを回すパターンB運用では対象範囲が特定できないため**採用しない**。
- 総括検収レポートには必ず統合ブランチ名と含まれるタスクIDを併記すること。

### 3.2 ブランチ運用フロー: パターン①（2026-07-27 正式決定）

**Julesの成果物は、ローカルの使い捨て統合ブランチへ順次 `pull --apply` して積載・検証し、
人間の最終承認時に一括で `master` へマージする。**

```
[Jules] master起点で作業 → パッチ生成
   ↓ jules remote pull --session <ID> --apply
[ローカル] integration/task-XX-YY へ積載（[WRITE-R]・タスクごとにコミット）
   ↓ pytest + git diff 静的レビュー（タスクごと）
[ブロック完了] 総括検収レポートを人間へ提出
   ↓ 人間の明示的一括承認 [WRITE-I]
[master] 一括マージ・プッシュ
```

- 途中で問題が判明した場合、**統合ブランチごと破棄すれば `master` は無傷**（完全な可逆性）。
- 旧フロー「1タスクごとに人間承認して `master` へ push」（パターン②A）は**廃止**する。

#### ⚠️ 本フローに伴う制約と対処（Jules 仕様に起因）
`jules new` に起点ブランチ指定フラグは存在せず、**Jules は常にリモートの `master` 最新状態から作業を開始する**。
したがってブロック内の後続タスクは、先行タスクの成果物が見えない状態で作業することになる。

**対処（必須）**: §7.6「指示書の自己完結原則」を全タスクに適用すること。

4. **Phase 4: 業務日報・進捗記録の自動更新 (Antigravity)**
   - ユーザーから「日報更新」「日報を最新化」などの指示を受けた際、または一連のタスク完了時には、Antigravity が自動的に `.nightly/DEVELOPMENT_LOG.md` を最新のタスク状況・知見・テストログで更新しコミットする。

---

## 🔒 4. 操作リスク分類

すべてのエージェント行動は事前に以下の3種類に分類され、権限の範囲内で実行されます。

| 分類 | 対象操作 | 権限 |
|---|---|---|
| **[READ]** | 検索、コード・ログ閲覧、状態確認、単体テスト実行 | 自由実行可 |
| **[WRITE-R]** | 管理ファイル更新 (`.nightly/`)、作業ブランチ作成、**使い捨て統合ブランチ (`integration/*`) へのコミット・マージ** | 自主実行可（記録・報告義務あり） |
| **[WRITE-I]** | **`master`マージ、`git push origin master`**、ファイル削除、認証変更、**`upstream`(NDL) への一切の書き込み** | **実行絶対禁止**（人間の明示的承認が必須） |

- 分類に迷った操作は、常に1段階厳格な **[WRITE-I]** として扱ってください。
- 不可逆操作（マージ・削除等）の権限はAIエージェントに委譲されません。
- ただし統合ブランチ (`integration/*`) は**人間承認前であればブランチごと破棄でき可逆**であるため [WRITE-R] とし、パターンBの自律連続処理を成立させます。**不可逆となるのは `master` へのマージ・プッシュのみ**です。
- `upstream`（国立国会図書館）への push は承認の有無にかかわらず**恒久的に禁止**とします（push URL を技術的に無効化済み）。

---

## ⛔ 5. 開発・テスト共通禁止事項

1. **既存テストの絶対保護**:
   - 新規機能・ロジック変更には必ず対応するテストを追加・更新すること。
   - **既存テストの削除・スキップ・期待値変更（テスト改変）を固く禁止**する。
   - 既存テストが失敗する場合は、テストコードを直してグリーンにするのではなく、「失敗内容と原因」を報告して停止すること。
2. **保護領域への不可侵**:
   - 許可されていない設定ファイル、本番構成、認証・シークレット領域を無断で変更しないこと。
3. **範囲逸脱（ついで改善）の禁止**:
   - 指示書に記載のないリファクタリング、フォーマット変更、別機能の修正を行わないこと。
4. **ベースソフトウェアのクレジット表示の絶対保護（ライセンス遵守）**:
   - 本プロジェクトは NDLOCR-Lite（国立国会図書館）を **CC BY 4.0** の下で利用・改変している。
   - **国立国会図書館の著作権表示・クレジット表示・ライセンス条項（`LICENCE` / `LICENCE_DEPENDENCEIES` / ソースコード先頭のヘッダコメント / README のクレジット記載等）を、削除・改変・移動してはならない。**
   - 「不要なコメントの整理」「ヘッダの統一」等を理由とした削除も**一切禁止**とする。ライセンス表示に関わる変更が必要と判断した場合は、実施せず人間に報告すること。
   - 改変を加えた箇所については、CC BY 4.0 の要求に従い「改変した旨」を明示できる状態を維持すること。
5. **モデル再学習の禁止（スコープ管理）**:
   - `train/` 配下の学習スクリプト実行およびモデル重みの再生成・差し替えは本プロジェクトのスコープ外とする。必要と判断した場合は実装せず提案に留めること。

---

## 📝 6. 報告・引き継ぎ義務

- **指示の曖昧さ・矛盾への対応**: 指示に曖昧さや矛盾を発見した場合、推測で埋めて実装を進めず報告・質問すること。
- **障害発生時の撤退**: 同一原因のエラーが2回発生した場合は作業を停止し、試行結果と障害分析を提示して人間に指示を仰ぐこと。

---

## 🚀 7. Julesへの発注指示書作成・発注規約（Antigravity側ルール）

指揮官（Antigravity）が実行部隊（Jules）へタスクを発注する際は、以下の作成規約および発注手順を遵守しなければなりません。

### 7.1 発注指示書の必須構成要素
すべての発注指示書には以下の5項目を明記すること：
1. **タスクIDおよびタイトル**: （例: `Task 08: [NEW] タスクの優先度設定機能`）
2. **目的・背景**: 実装する機能の必要性と概要
3. **検証可能な完了条件 (DoD)**: 曖昧さを排除し、単体テストおよびUI挙動で判定可能な条件（3項目程度）
4. **変更許可スコープ（ホワイトリスト） & 変更禁止パス（ブラックリスト）**: 変更を認めるファイル群と絶対不可侵な管理ファイルを明示
5. **既存テスト絶対保護規定**: 既存単体テストの削除・スキップ・期待値変更の禁止、新規テスト追加の要求

### 7.2 自律連続発注手順
1. **ROADMAP更新**: `.nightly/ROADMAP.md` に未完了タスク（`[ ] Task XX`）を登録する。
2. **Jules Sessionトリガー**: Jules CLI / `scripts/jules_session.py` またはセッションAPI経由で指示書を投稿し、Julesのセッションを起動する。
3. **30分タイマー起動**: 発注完了直後、必ず `schedule` ツールで「30分タイマー（DurationSeconds: 1800）」をセットしてターンを終了する。
4. **イベント監視・自律検収**: タイマー起動または即時リアクティブイベント受信時に、`jules remote pull` で成果物を自動取得・単体テスト検証する。

### 7.3 Jules CLI 標準発注コマンド手順（2026-07-27 実環境で検証済み）
Jules CLI は **npm 経由で PATH に導入済み**（`C:\Users\user\AppData\Roaming\npm\jules.ps1`）。旧記載の `%TEMP%\jules_tmp\jules.exe` は**存在しないため使用禁止**。

```powershell
# 1. 指示書テキストファイルの作成
#    .nightly/prompts/taskXX_prompt.txt

# 2. 標準入力パイプによる発注
Get-Content .nightly\prompts\taskXX_prompt.txt -Raw | jules new --repo a19156664-bot/ndlocr-lite-custom

# 3. セッション一覧の確認
jules remote list --session

# 4. リモート結果の自動引き戻し＆ローカル適用
jules remote pull --session <SESSION_ID> --apply
```
> ⚠️ 日本語プロンプトを扱うため、実行前に `$env:PYTHONIOENCODING="utf-8"` を設定すること（Windows PowerShell の cp932 対策）。

### 7.6 指示書の自己完結原則（パターン①の必須補完ルール）

Jules は常に `master` 起点で作業するため、**同一ブロック内の先行タスクの成果物を見ることができない**。
指揮官は、依存関係のあるタスクを発注する際、以下を指示書に**必ず**含めること。

1. **依存先のインターフェース契約を、指示書内に全文明記する。**
   「Task 02 が作った関数を使え」ではなく、**関数名・引数・戻り値の構造・フィールド名と型**を
   指示書にそのまま書き下す。参照ではなく複製で渡すこと。
2. **依存先ファイルが存在しない前提で書く。**
   「`custom_gui/ocr_bridge.py` が存在しない場合は、上記契約に従う最小限のスタブを作成してよい」と明記する。
3. **担当外ファイルへの変更を明示的に禁止する。**
   並列発注する各タスクの担当ファイルを相互に排他とし、
   「`custom_gui/viewer.py` は別タスクが担当中のため触れないこと」のように相互排他を明記する。
4. **契約に齟齬がある場合は実装せず報告させる。**
   指示書の契約と `master` の実態が食い違う場合、推測で埋めずに報告して停止させる。

> この原則を守らない発注は、統合時のファイル衝突とデグレを高確率で発生させる。
> 指示書作成時のチェック項目として扱うこと。

### 7.4 義務事項: 発注直後の30分タイマー（schedule）絶対設定規約（再発防止ルール）
指揮官（Antigravity）は、Julesへのタスク発注（セッション創出）を完了した後、**必ず例外なく `schedule` ツールを用いて「30分タイマー（DurationSeconds: 1800, Prompt: 'Julesの成果物チェックタイマー'）」を設定してからスリープに入らなければならない**。
これにより、放置事故を100%防ぎ、自律的な定期検収ループを維持・保証する。

---

## 📜 8. 承認者の判断の記録

**ここに書いたことは、再び尋ねない。**
各項目には「どうなったらこの判断が無効になるか」を必ず添える。添えていない判断は、
状況が変わっても古いまま使われ続けるため、記録として不完全である。

### 8.1 手描きマークの色の意味（2026-08-27）

承認者が誌面に手描きしたマーカーの色には、次の意味がある。

| 色 | 実測 HSV | 意味 | 対応する実装 |
|---|---|---|---|
| **水色** | H=90 / S=157-159 / V=233-238 | **OCR させたい範囲** | `mark_detector.detect_marks()` |
| **橙色** | H=25 / S=159 / V=236-239 | **そのページは広告** | `page_marks.MARK_AD`（`【広告】`） |

水色には形が2種類あり、**どちらも OCR 範囲として扱う**（2026-08-27 のご判断）。

    囲み（closed loop） … 縦横比 0.56-3.29。そのまま矩形にする
    傍線（highlighter） … 幅 11-12px。細い方向へ左右 20px 広げる
                          （帯 12px に対し縦書きの見出し列は実測 48px）

**分離できる根拠（`jpg/` 52枚・2026-08-27 実測）**

    橙色が 2000 画素以上あるのは 9 ページのみ（3,506-8,411 画素）
    残り 43 ページの橙色は、合計で 10 画素
    紙・活字は彩度 2-5、題字の印刷インクは彩度 68・明度 88
    → S>=120 かつ V>=180 で、手描きだけが残る

**この判断が無効になる条件**

- 承認者が3色目のマーカーをお使いになったとき
- 別の資料（別誌・別スキャナ）を扱うとき。**しきい値は測り直すこと。**
  上の数値は『國際寫眞新聞』第28号のスキャン52枚から得たものである

### 8.2 マーク検出に LLM を使わない（2026-08-27）

Gemini などの LLM でマークを囲む案が出たが、**色検出を採用した**。

理由は3つ。**決定論的でないと本 AGENTS の関門（DoD を数値で書き、欠陥再注入で
検査が本当に見分けるか測る）が成立しない。** LLM の bounding box の座標誤差は、
範囲OCR では行の欠落に直結する。加えて課金が発生し、オフラインで動かなくなる。

**この判断が無効になる条件**

- 色で分離できないマーク（鉛筆・付箋・折り目など）を扱う必要が生じたとき
- OCR 結果の校正や読み順の判定など、決定論的に解けない工程を足すとき。
  **そこは LLM の値打ちが出る場所であり、この判断は及ばない。**
