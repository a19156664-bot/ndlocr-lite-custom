# プロジェクト マスターロードマップ (ROADMAP.md)

本ロードマップは、全タスクの分解および承認・完了状態を管理するマスタードキュメントです。

---

## 🎯 プロジェクト目標
- **成果物**: NDLOCR-Lite をベースに、**マウスで矩形範囲を選択してその範囲だけOCRする**デスクトップアプリ
- **第1弾ゴール**: 実用アプリとして一通り（PDF対応・ページ送り・複数矩形・保存・フォルダ一括）
- **方式**: C方式（fork ＋ 追加モジュール）。実装は `custom_gui/` と `tests/` のみ
- **ステータス**: 🟡 進行中

---

## 🧭 設計の前提（2026-07-27 実機検証で確定）

- `src/ocr.py` は1枚あたり約6秒で `.txt` / `.json` / `.xml` を出力する（実測: `resource/digidepo_2531162_0024.jpg`）。
- **`.json` は行単位で `boundingBox`（4隅の "x y" 文字列）・`text`・`confidence`・`isVertical` を持つ。**
- したがって**範囲指定OCRは「全面OCR → boundingBox による矩形内フィルタ」で実現する**ことを基本方針とする。
  部分画像を切り出して再推論する方式は、文脈欠落による精度低下リスクがあるため第1弾では採らない。

### 追加確定事項（2026-07-27 人間承認済み）

1. **1画像内に複数の選択矩形を保持する。**
   矩形は最初から**リスト構造（各矩形が一意のID・ラベルを持つ）**として設計する。
   単一矩形前提で実装してから複数対応へ作り直すことを**禁止**する（Task 04 の時点でリスト構造とすること）。
2. **OCR結果には必ず「元画像のファイル名」をセットで保持・出力する。**
   フォルダ一括処理（Task 12）で結果がどのファイル由来か追跡できることが必須のため、
   Task 02 のデータ構造の時点で `source_image` フィールドを持たせる。
   txt/CSV出力（Task 09）にも画像ファイル名を含めること。

---

## 📋 タスク分解キュー

### ブロック1: 基盤（統合ブランチ `integration/task-01-03`）
> 終端の状態: 画像を開いて全面OCRを実行し、結果を取得できる

### [ ] Task 01: [NEW] custom_gui パッケージ雛形とテスト基盤の構築
- **DoD (完了条件)**:
  - [ ] `custom_gui/__init__.py` が作成され、`python -c "import custom_gui"` が成功する
  - [ ] `tests/` ディレクトリと `tests/test_smoke.py` が作成され、`python -m pytest tests/ -v` が1件以上グリーンで完走する
  - [ ] 上流ファイル（`src/` `ndlocr-lite-gui/` `train/` `LICENCE*`）に一切の変更がない

### [ ] Task 02: [NEW] OCRブリッジ（src/ocr.py 呼び出しと結果の構造化）
- **DoD (完了条件)**:
  - [ ] `custom_gui/ocr_bridge.py` に、画像パスを受け取りOCRを実行して行データのリストを返す関数を実装
  - [ ] 各行が `text` / `bbox`(x1,y1,x2,y2 の数値タプル) / `confidence` / `is_vertical` / **`source_image`（元画像のファイル名）** を持つ形に正規化される
  - [ ] `resource/digidepo_2531162_0024.jpg` を入力とするテストが追加され、1行以上が取得でき、かつ全行の `source_image` が入力ファイル名と一致することを検証してグリーン

### [ ] Task 03: [NEW] 画像ビューア（表示・ズーム・フィット）
- **DoD (完了条件)**:
  - [ ] `custom_gui/viewer.py` に flet で画像ファイルを表示するコンポーネントを実装
  - [ ] 表示倍率の変更と「ウィンドウに合わせる」が動作する
  - [ ] 表示座標 ⇄ 原画像座標の相互変換関数を実装し、往復変換の一致を検証するテストがグリーン

---

### ブロック2: 核心機能（統合ブランチ `integration/task-04-06`）
> 終端の状態: **範囲指定OCRが成立する**

### [ ] Task 04: [NEW] 矩形ドラッグ選択UI
- **DoD (完了条件)**:
  - [ ] 画像上でマウスドラッグすると矩形が描画され、確定できる
  - [ ] 確定した矩形が Task 03 の変換関数により**原画像座標**として取得できる
  - [ ] **矩形は一意のIDを持つリスト構造で保持され、1画像に複数の矩形を追加できる**（単一矩形前提の実装を禁止）
  - [ ] 矩形の正規化（右下→左上方向のドラッグ、画像外へのはみ出し）および複数矩形の追加・保持を検証するテストがグリーン

### [ ] Task 05: [NEW] 範囲フィルタ（boundingBox による行の絞り込み）
- **DoD (完了条件)**:
  - [ ] 選択矩形と各行の `bbox` の重なりを判定し、範囲内の行のみを返す関数を実装
  - [ ] 判定方式は「行の中心点が矩形内」または「重なり面積が行面積の50%以上」とし、いずれかを選択して根拠をコード内コメントに明記
  - [ ] 完全内包・部分重なり・完全に外側・境界接触の4ケースを網羅するテストがグリーン

### [ ] Task 06: [NEW] 範囲OCR結果の表示とハイライト
- **DoD (完了条件)**:
  - [ ] 選択範囲を確定すると、その範囲内のテキストが結果パネルに表示される
  - [ ] 抽出対象となった行が画像上でハイライト表示される
  - [ ] 縦書き（`is_vertical=true`）の資料でも読み順が崩れないことを `resource/tategaki2026-04-24-094138.png` で確認

---

### ブロック3: 実用化（統合ブランチ `integration/task-07-09`）
> 終端の状態: 実務で使える

> ⚠️ **タスク番号の実績との対応について**
> ブロック2で不具合修正が連続したため、実際に発注した Task 07〜10 は当初計画と内容が異なる。
> 実績: Task 07/08/09/10 はいずれも [FIX]（表示不具合・レイアウト・予約名衝突の修正）に費やされた。
> **2026-07-27 時点で Task 01〜10 が完了し、master (4158478) で実機動作を確認済み。**
> 以降のタスク番号は Task 11 から連番で払い出す。

---

### ブロック3: 操作性の向上（人間の要望に基づき 2026-07-27 追加）
> 終端の状態: 資料を快適に読み進められる
>
> **実行順序（人間の判断・2026-07-27）**: Task 11（画像移動） → **Task 12（OCR非同期化）** → Task 13（Ctrl+ホイール） → Task 14（+-キー）
> 起動時53秒のフリーズ解消を、残る操作性向上より優先する。

### [ ] Task 11: [NEW] モード切替ボタンによる画像の移動（パン）
- **背景**: 拡大時に画像の見たい箇所へ移動できず、ユーザー体験を損ねている
- **当初案からの変更（2026-07-27）**: 「Ctrl + ドラッグ」は **flet 0.27.6 の API 制約により実現不可能**と判明。
  - `DragStartEvent` / `DragUpdateEvent` / `DragEndEvent` は修飾キー状態を持たない（ソース確認済み）
  - `on_keyboard_event` は存在するが**キー解放イベントが無く**、Ctrl の押下継続を追跡できない
  - Jules がフェイルセーフを発動して報告・停止 → 指揮官が検証 → 人間が**案A（モード切替）**を選択
- **DoD (完了条件)**:
  - [ ] ツールバーに「移動 / 選択」のモード切替ボタンがあり、現在のモードが一目で分かる
  - [ ] **移動モード**ではドラッグで画像がその方向へ動く
  - [ ] **選択モード（既定）**では従来どおり矩形選択として動作する（中核機能を壊さない）
  - [ ] 移動後も確定済み矩形とハイライトが画像上の正しい位置に追従する
  - [ ] 移動量が `offset_x` / `offset_y` に反映され、座標変換の往復が成立することをテストで検証

### [ ] Task 12: [NEW] OCRの非同期化と進捗表示
- **背景**: 起動時にOCRを同期実行するため**約53秒間UIが応答せず**、✕ ボタンも効かない（実測値 52.8秒）
- **優先度**: 最高（人間の判断により Task 11 の次に前倒し。2026-07-27）
- **DoD (完了条件)**:
  - [ ] 起動直後に画像・ボタン・パネルが表示される（OCR完了を待たない）
  - [ ] OCR実行中も**ウィンドウを ✕ で閉じられる**
  - [ ] 「OCR処理中」の進捗表示があり、完了時にステータスバーが `Lines: 26` へ更新される
  - [ ] OCR完了前にドラッグしても異常終了しない

### [ ] Task 13: [保留] Ctrl + マウスホイールによる拡大・縮小 — **実現不可能**
- **調査結果（2026-07-27・指揮官が flet ソースで確認）**:
  `ScrollEvent` が持つのは `global_x/y` `local_x/y` `scroll_delta_x/y` のみで、**修飾キー状態を持たない**。
  Task 11 と同じ制約であり、Ctrl の押下継続も追跡できないため実現できない。
- **代替案（人間の判断待ち）**:
  - (a) 移動モード中のホイールを拡大縮小に割り当てる（モードで意味を切り替える）
  - (b) ホイールはスクロール専用のままとし、拡大縮小は Task 14 のキーとボタンに委ねる
  - (c) `ft.InteractiveViewer` への載せ替え（ホイール拡縮が標準で付くが矩形選択との共存に難）

### [ ] Task 14: [NEW] キーボードショートカット（+ で拡大 / - で縮小）
- **実現可能性**: ✅ `KeyboardEvent` は `key` / `ctrl` / `shift` / `alt` / `meta` を持つため実装可能（確認済み）
- **DoD (完了条件)**:
  - [ ] `+` キーで拡大、`-` キーで縮小する
  - [ ] テキスト入力中など、意図しない場面で発火しないこと
  - [ ] （併せて検討）モード切替のショートカットキー

---

### ブロック4: 実用機能（第1弾ゴールDの残り）

### [ ] Task 15: [FIX] tests/test_ui_build.py の有効化
- **背景**: 現在のテストは欠陥を再注入しても PASS してしまい、再発防止の役目を果たしていない（検収時に実証）
- **方針**: サブプロセスで `ft.app(view=FLET_APP_HIDDEN)` を実際に起動し、例外なく完了することを検証する

### [ ] Task 16: [NEW] 矩形のリサイズ・移動（編集機能）
### [ ] Task 17: [NEW] txt / CSV 出力（**画像ファイル名・矩形IDを列に含めること**）
### [ ] Task 18: [NEW] ファイル選択ダイアログ
### [ ] Task 19: [NEW] PDF読み込みとページ送り
### [ ] Task 20: [NEW] フォルダ一括処理（同一矩形の適用と結果集約）
> ※ DoD は着手時点の実態を踏まえて確定する（推測でDoDを書かない）

---

---

## ✅ 2026-07-27 深夜〜28 早朝 実施分（パターンB 一括承認待ち）

| Task | 内容 | 状態 |
|---|---|---|
| 11 | モード切替（選択/移動）による画像パン | ✅ merged `5c58170` |
| 12 | **OCR非同期化（起動時53秒フリーズの解消）** | ✅ merged `662e353` |
| 12b | 非同期OCR回帰テストの実効化 | ✅ merged `02256f5` |
| 21 | Region ラベルを矩形の外側へ | ✅ merged `03d5f43` |
| 22 | **CSV/TXT 出力（画像名込み・BOM付きUTF-8）** | ✅ merged `57ca1fb` |
| 23 | **フォルダ読込・画像切替・Prev/Next/Ctrl+N** | ✅ merged `f4b16df` |
| 24 | **F2 によるOCRテキスト修正** | ✅ merged `4898c22` |
| 23b | — | ⛔ スコープ違反により差し戻し（未マージ） |
| 23c | クラッシュ修正＋全画像出力の純粋ロジック | ✅ merged `0bb80e9` |
| 23d | 全画像一括出力のUI結線 | ✅ merged `02d0546` |

- テスト件数: 29 → **65**
- 実行順序は人間の判断（2026-07-27）に基づく: 12 → 21 → 22 → 23 → 24

### 実機確認が必要な項目

1. **F2 キーが実際に発火するか**。flet のキー文字列は Flutter クライアント側から送られるため、
   打鍵なしには `e.key == "F2"` を確定できなかった。EDIT ボタン経由の編集は検証済み。
2. Select/Pan トグル、Region ラベル位置、出力メニュー、Prev/Next の見た目。

### 既知の課題（次回以降）

- Task 13（Ctrl+ホイール拡縮）は flet 0.27.6 では **実現不可能**。代替案は人間の判断待ち。
- Task 14（+/- キー拡縮）は実現可能・未着手。
- Task 15（tests/test_ui_build.py の有効化）は未着手。
- テスト実効性の弱さが複数タスクで確認された（BOM書込・状態保持・編集処理など、
  テスト側が実処理を模倣しており退行を検出できない）。指揮官の headless 検証で代替中。

## 🚧 全タスク共通の制約

- **変更許可スコープ**: `custom_gui/**` および `tests/**` のみ
- **変更禁止パス**: `src/**` `ndlocr-lite-gui/**` `train/**` `resource/**` `LICENCE` `LICENCE_DEPENDENCEIES` `README.md` `.github/**` `.nightly/**` `AGENTS.md` `scripts/**` `pyproject.toml` `requirements.txt`
- **既存テストの削除・スキップ・期待値変更を禁止**（AGENTS.md §5-1）
- **NDLのクレジット・ライセンス表示の削除・改変を禁止**（AGENTS.md §5-4）
