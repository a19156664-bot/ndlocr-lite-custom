# 📜 ループエンジニアリング 業務日報 兼 開発進捗記録簿

**プロジェクト名**: NDLOCR-Lite カスタマイズ OCR アプリ（範囲指定OCR）
**運用モード**: パターンB（パイプライン一括検収・まとめ承認モデル）
**最新更新日時**: 2026-07-30
**進捗ステータス**: 🟢 実用段階（master 6d079b6・230 tests passed）

---

## 📊 1. タスク進捗サマリー一覧

| タスクID | 機能・タスク概要 | 担当 | 検証結果 | 統合状態 | 備考 |
|---|---|---|---|---|---|
| **Task 01** | custom_gui 雛形とテスト基盤 | Jules | Pending | 未統合 | ブロック1 |
| **Task 02** | OCRブリッジ（結果の構造化） | Jules | Pending | 未統合 | ブロック1 |
| **Task 03** | 画像ビューア（表示・座標変換） | Jules | Pending | 未統合 | ブロック1 |
| **Task 04** | 矩形ドラッグ選択UI | Jules | Pending | 未統合 | ブロック2 |
| **Task 05** | 範囲フィルタ（bbox絞り込み） | Jules | Pending | 未統合 | ブロック2 |
| **Task 06** | 範囲OCR結果の表示・ハイライト | Jules | Pending | 未統合 | ブロック2 |
| **Task 07-09** | 複数矩形・一覧・出力 | Jules | Pending | 未統合 | ブロック3（DoD未確定） |
| **Task 10-12** | PDF・ページ送り・一括処理 | Jules | Pending | 未統合 | ブロック4（DoD未確定） |

---

## 📝 2. 本日の業務日報（2026-07-27）

### 📌 実施業務内容
1. NDLOCR-Lite のライセンス調査（一次情報で確認）— CC BY 4.0、ソース・学習スクリプト共に公開、依存ライブラリに制約ライセンスなし
2. 要件確定（3問の認識統一）— ①手動矩形選択方式 ②C方式(fork+追加モジュール) ③第1弾ゴールは実用アプリ一式
3. `ndl-lab/ndlocr-lite` を `a19156664-bot/ndlocr-lite-custom` へ fork、`C:\Users\user\ndlocr-lite-custom` へ clone
4. `upstream` remote 登録および **push URL の無効化**（NDLリポジトリへの誤 push 防止）
5. スターターキット（AGENTS.md / CLAUDE.md / task_template.md / .agents / .nightly / scripts）を配置
6. AGENTS.md をプロジェクト固有値へ更新（リポジトリ名・`master`ブランチ・pytest・Jules CLI実パス）
7. Python 仮想環境構築と依存インストール（Python 3.13.5 / 全32パッケージ成功）
8. **OCR実機検証に成功**（`resource/digidepo_2531162_0024.jpg` を6.0秒で処理、txt/json/xml 出力）
9. `.nightly/ROADMAP.md` に4ブロック12タスクを分解・登録

---

## 🧠 3. 課題と獲得した知見 (Lessons Learned & Gotchas)

* **知見1: OCR結果JSONに行単位の `boundingBox` が含まれる。** → 範囲指定OCRは「全面OCR → 矩形内フィルタ」で実現可能。部分画像を切り出して再推論する必要がなく、実装が大幅に簡素化される。ROADMAPの設計前提に反映済み。
* **知見2: fork のデフォルトブランチは `main` ではなく `master`。** 憲法の保護対象ブランチ名を `master` に修正済み。以後 `git push origin main` は存在しないブランチへの操作となるため注意。
* **知見3: Jules CLI の実体は npm 経由の `C:\Users\user\AppData\Roaming\npm\jules.ps1`。** 旧AGENTS.mdに記載の `%TEMP%\jules_tmp\jules.exe` は**存在しない**。§7.3 を実環境に合わせて修正済み。
* **知見4: gh CLI はインストール済み・認証済み**（`a19156664-bot`、スコープ `gist, read:org, repo`）。ただし **`workflow` スコープがない**ため、`.github/workflows/` を変更する push は失敗する見込み。CI設定を触る場合は再認証が必要。
* **知見5: モデルファイル（.onnx 計約150MB）は Git LFS ではなく実体でコミットされている。** clone だけで動作する反面、リポジトリが重い。
* **知見6: 上流の `.gitignore` は `.venv` `__pycache__` を既に除外済み。** 失敗モードカタログ A-3（不要ファイル混入）の予防が上流側で完了している。
* **知見7: Python 3.13.5 で全依存パッケージがインストール可能。** `requirements.txt` は 3.11以上に onnxruntime 1.26.0 を割り当てており、3.13でも問題なく動作した。

---

# 📜 業務日報（2026-07-30）— 永続化と保存不具合の解消

**master**: `6d079b6`
**テスト**: **230 passed**（前回セッション開始時 191 → 230、+39）
**進捗ステータス**: 🟢 実用段階。中断・再開が可能になった

## 📊 本セッションのマージ履歴

| コミット | 内容 | テスト |
|---|---|---|
| `50122e7` | Task 33b 矩形横のインラインエディタ | |
| `3a22872` | Task 35 + 35b 保存アイコン2種・保存先ダイアログ廃止・改行数表示 | |
| `b4907f8` | Task 36 + 36b 広告 / 表紙 ボタン | |
| `8cbe6b4` | Task 37 右クリックで Pan 切替・カーソルでモード可視化 | |
| `7b1b2bb` | Task 38 Ctrl+S 保存・Enter 確定・次ページ確認 | 184 → 191 |
| `07bec90` | chore `resource/*.csv` `*.txt` を .gitignore | 191 |
| `ed96acf` | **Task 39 系** 矩形・修正文字・マークの永続化 | 191 → 215 |
| `ed96acf` | **Task 40** 保存の拒否が見えない不具合の修正 | 215 → 227 |
| `6d079b6` | **Task 41** Pan カーソルの Windows 対応 | 227 → 230 |

## 📌 実施業務内容

### Task 39 / 39b / 39c — 作業状態の永続化（3回の差し戻しを要した）

`custom_gui/work_state.py`（新規・純粋）を追加し、矩形・F2/インライン修正・
広告/表紙マークを `.ndlocr_cache/<画像名>.work.json` へ**操作のたびに原子的に
保存**、起動時に復元する。`ocr_cache.py` と同じ方式（tempfile + os.replace、
schema、size/mtime による陳腐化検知）を踏襲。PDF ページは PDF 本体の隣に
`<PDF名>_p0004.work.json` として保存する（レンダリング先の一時フォルダは
終了時に消えるため）。

- Task 39: `Failed`。import 漏れ・復元処理なし・結線3/6箇所。実機検証 7項目中7項目 FAIL
- Task 39b: 機能は成立（22項目通過）。ただし範囲外のガード1行で既存13件が破損
- Task 39c: テストのハンドラ経由化は成功。ただし `app.py` を構文エラーにした
- 最終的にユーザー承認のもと、指揮官が `app.py` をガード2行の削除のみに差し戻した

### Task 40 — 「このページ保存・Ctrl+S が効かない」の修正

ユーザー報告。真因は**拒否がステータス欄の英字表示だけで見えないこと**。
ユーザー自身が「OCR（select）を行っていない場合」と特定した。

`_start_export("current")` の結末を4通りに整理した。

| 状態 | 変更後 |
|---|---|
| 矩形もマークも無い | モーダル「OCRデータがございません。」 |
| 矩形あり・OCR未完了 | `_pending_export` に予約し、OCR完了後に自動保存 |
| 矩形あり・OCRエラー | ダイアログでエラー内容を表示 |
| 上記以外 | 確認なしで即上書き |

`_show_overwrite_dialog` はメソッドごと削除。

### Task 41 — Pan のカーソルが Windows で手にならない

`ft.MouseCursor.GRAB` / `GRABBING` は **Flutter の Windows embedder に
マッピングが無く、既定の矢印にフォールバックする**（flutter/flutter #99323、
P2、2026年7月時点で Open）。`CLICK`（IDC_HAND 指差しの手）と
`ALL_SCROLL`（IDC_SIZEALL 四方向矢印）に差し替えた。

### その他

- `.nightly/verify/` を新設し、指揮官の検収ツール26本を保全（従来はセッション固有の一時ディレクトリにあり、セッションをまたぐと失われていた）

---

## 🧠 課題と獲得した知見（2026-07-30）

* **知見8: `except Exception: pass` は import 漏れを隠蔽する。**
  Task 39 は `work_state` を import せずに `work_state.save_work_state()` を
  呼び、その `NameError` を自分で書いた `except Exception: pass` が飲み込んだ。
  **ユーザーには保存されたように見えて、ディスクには何も書かれていない。**
  指示書で「書き込み失敗は握り潰せ」と書いたことが蓋になった。
  → 以後、握り潰しは **`except (OSError, TypeError, ValueError)` のように
  対象を限定**させ、プログラムの誤り（NameError / AttributeError /
  ImportError）は落ちるようにする。「NameError が伝播すること」を
  `pytest.raises` で検証するテストを必須にした。

* **知見9: 「メソッドが呼ばれたこと」を検証するテストは結線漏れを検出できない。**
  Task 39b のテスト11件はすべて `_rects.append` と `_persist_work_state()` の
  直接呼び出しでハンドラを迂回しており、6種の欠陥注入のうち**4種が素通り**した。
  → **`os.path.exists` でファイルの実在を確認**させ、`_on_pan_end` /
  `delete_rect` / `btn_mark_ad.on_click` / `_switch_image` といった
  **実ハンドラを発火**させることを指示書で個別に指定する。到達方法（既存の
  `test_edit_enter.py` / `test_pan.py` の手法を流用せよ）まで書くと成功率が上がる。

* **知見10: flet の enum に値が存在することは、その値が動作することを意味しない。**
  Task 37 で `ft.MouseCursor.GRAB` の存在だけを確認し、プラットフォーム対応を
  検証しなかった。Windows には開いた手・握った手のカーソルが**そもそも存在
  しない**。Win32 の手は `IDC_HAND`（指差し）だけ。
  → 以後、プラットフォーム依存の値は **Flutter 側の実装状況まで確認**する。
  設定しても例外が出ずアサーションも通る値は機能テストで検出できないため、
  `tests/test_flet_constants.py` にガードを追加した。

* **知見11: Jules は「2行削除せよ」に3回失敗し、4行削除して構文エラーにした。**
  消えたのは `with self.selections_lock:`（Task 30c の重大修正）と
  `if hasattr(self, 'mark_label'):`（Task 36 のガード）。
  → 削除系の指示は**削除対象の行を完全に引用**し、隣接する保護対象の行を
  名指しで「絶対に消すな」と書く。それでも失敗し得るため、`ast.parse` を
  検収の最初に回す。

* **知見12: Jules は指示書ファイル自身を削除する。**
  Task 39b・39c が `.nightly/prompts/taskXXb_prompt.txt` を削除した。
  → 指示書の冒頭に「DO NOT DELETE THIS FILE」を書き、`.nightly/` への変更を
  全面禁止した。検収時は必ず `ls .nightly/prompts/taskXX_prompt.txt` で確認する。

* **知見13: 作業用ファイルの残置は常態。** Task 35 が20個、36 が16個、39 が10個、
  39b が3個、39c が4個。39c の `test_diff.py` `test_tmp.py` 等は
  **`test_` 始まりでリポジトリ直下に置かれるため pytest が収集してしまう**。
  → 指示書で実名を列挙し、「スクラッチはシステムの一時ディレクトリへ。
  `test_*.py` の名前を絶対に付けるな」と明記した。Task 40 / 41 では0個になった。

* **知見14: 提出前の `pytest --collect-only` は必須。** Task 39 の
  `test_persistence.py` は22行目が `IndentationError` で、**収集段階で
  スイート全体が停止**していた。実機検証7項目が丸ごと未実施のまま「納品」された。

* **知見15: 検収ツールを一時ディレクトリに置くと失われる。**
  `verify*.py` は毎回作り直していた。`.nightly/verify/` に移し、README で
  使い方を残した。

* **知見16: 上書き確認に autofocus を付けないという判断が、キーボード操作の
  行き止まりを作った。** Task 38 で「Enter 連打で未確認のファイルを潰す事故を
  防ぐ」ため意図的に外したが、**2回目以降の保存は必ず上書き確認を通る**ため
  `Ctrl+S` → `Enter` が毎回死んだ。広告/表紙ボタンが押した瞬間に
  `<画像名>.txt` を作ることも効いていた。
  → 安全側に倒す判断は、**その状態が「例外」なのか「常態」なのか**を確認する。

* **知見17: 無効なテストは通算12件。** いずれも「検証対象そのものをモック
  または迂回で置き換える」パターン。欠陥再注入を全指示書で必須にしてから
  検出できるようになったが、**注入する欠陥の選び方**が重要。実際に出荷された
  欠陥と同じ形（結線漏れ・握り潰し）を注入すること。

---

# 📜 業務日報（2026-08-27）— 実機確認の決着と、母屋への追随

**指揮官は Claude（Claude Code）。** 前回（2026-07-30）から28日ぶりの再開。

## 📊 本日のマージ履歴

| コミット | 内容 |
|---|---|
| `0bc546e` | 破壊試験の台帳 `inject*.py` 8本をリポジトリ外へ移す |
| `472bd02` | 指揮官の手順書4本を `.claude/commands/` に新設 |

## 📌 実施業務内容

### 1. 会話履歴の所在を特定

この箱の作業ログは `~/.claude/projects/c--Users-user-ndlocr-lite-custom/` には無く、
**`c--Users-user-Opus5/` 側**（33,674行・2026-07-26〜08-04）にあった。当時 Claude Code を
`C:\Users\user\Opus5` から起動していたため。ndlocr の実作業は 2026-07-30 18:27 で終わり、
そこから moto-booklm へ話題が移っていた。

### 2. 母屋 `C:\Users\user\jules-pipeline` との差分調査

母屋の憲法は 2026-08-15 更新（19,372 B・9条）、この箱は 2026-07-27（16,061 B・7章）。
**18日分の差**があり、その中身は「約束が道具に置き換わった」ことだった。

### 3. 台帳をリポジトリ外へ（承認者のご判断・案ア）

`inject*.py` 8本を `C:\Users\user\ndlocr_sabotage\` へ移した。md5 で 8/8 の同一性を確認し、
移動後に 230 passed を確認。**Jules は clone するため、リポジトリ内の台帳は読める。**

### 4. 指揮官の手順書4本を新設（承認者のご判断・案ア）

`/発注` `/検収` `/抜取検査` `/記録` を、この箱の実物に合わせて書き下ろした。母屋の写しでは
ない。**この箱に無い道具（preflight.py / order.py / sabotage.py / dead_tests.py /
merge_if_gates_pass.py / heartbeat.py / notify.py / replywait）は手順として書かず、
「無いので機械が止めてくれない」と明示するに留めた。**

母屋とわざと違えた点：**master マージと push は [WRITE-I] のまま**とした。母屋は
「4つの関門を機械が測ればマージしてよい」へ改めたが、測る道具がこの箱に無い。

### 5. 実機確認 — 5項目 合格（承認者が実施）

| 項目 | 結果 |
|---|---|
| Task 41 Select＝十字 / Pan待機＝手 / Panドラッグ＝四方向矢印 | ✅ 3件 |
| Task 40 矩形なし保存 → 「OCRデータがございません。」 | ✅ |
| Task 40 同一ページ2回保存 → 確認なしで保存 | ✅ |

**残り6項目（Task 40 の 3〜5、Task 39 の 6〜8）は未確認。** ROADMAP に残した。

## 🧠 課題と獲得した知見（2026-08-27）

* **知見18: 台帳の置き場は、実装者から見えるかどうかで決まる。**
  欠陥再注入という思想は Task 35 から実践していたが、台帳を `.nightly/verify/` に
  置いていた。Jules は clone するため、**どこに欠陥を注入して検査するかを実装前に読めた**。
  思想が正しくても、置き場が間違っていれば検査は成立しない。

* **知見19: 「2回で止まる」は、数える者がいなければ効かない。**
  AGENTS.md §6 は「同一原因のエラーが2回で停止」と定めているが、この箱には数える機械が
  無い。母屋は 2026-08-14 に同じことに気づいている——「憲法も state.yml も『2で止まる』と
  書いていたが、何回失敗しても永遠に発注できる状態だった」。

* **知見20: テストが確かめていたのは「値が一致すること」で、「絵が出ること」ではなかった。**
  Task 37 はテスト緑でマージされたが、実機で Pan のカーソルが手にならなかった。flet が
  描画できない値（`GRAB` / `GRABBING`）だったが、テストは値の一致しか見ていない。
  注入を何本足してもこの型は見つからない。**実機確認は代替できない。**

* **知見21: 作業ツリーは消える。台帳の絶対パスは、それに気づかせてくれない。**
  `C:\Users\user\ndlocr-work` は現在存在しない（`git worktree list` にも出ない）。
  台帳 `inject*.py` はそこを絶対パスで指すため、**現状のままでは動かない**。
  再開時は `git worktree add` で作り直し、先頭定数を合わせること。

---

## 📌 Task 42 — 既存の矩形をタップしてインラインエディタを開く（2026-08-27 夜）

| 項目 | 値 |
|---|---|
| Jules セッション | `5927534187871357157` |
| 所要 | 19:55 発注 → 20:21 Completed（**26分**） |
| マージ | `1101558`（`ff09ba6` + `ee7a3f9`） |
| テスト | 230 → **240 passed**（マージ後の master 本体でも確認・終了状態 0） |
| 変更ファイル | 3件（`selection.py` / `app.py` / `tests/test_tap_to_edit.py`） |

**実装**：`find_region_at_point()` を `selection.py` に追加（重なりは小面積優先・境界は内側・
`scale=0` でも例外なし）。`app.py` の GestureDetector に `on_tap_up` を結線。

### 指揮官が塞いだ穴（`ee7a3f9`）

破壊試験 `no_mode_guard`（SELECT ガード除去）が**検出できなかった**。原因を測ると、
`test_handler_pan_tap_inside` は `zoom_scale` を設定しておらず、既定値 **0.48** のため
タップ (30,30) が元画像 (62.5, 62.5) へ変換され、矩形 (10,10,50,50) の外に落ちていた。
**ガードを一度も通らないテスト**だった（無効なテスト・通算13件目）。

`zoom_scale=1.0` と offset を明示する2行を追加。先にガードを外して FAIL を見てから書いた。

## 🧠 獲得した知見（Task 42）

* **知見22: `on_tap` は座標を持たず、`on_tap_down` はドラッグ確定前に発火する。**
  flet 0.27.6 のソースを読んで確定した（記憶ではない）。`on_tap_down` を選ぶと、
  矩形を描くたびにエディタが開いて閉じる。**タップ座標が要るなら `on_tap_up`。**

* **知見23: 実物の空行には末尾の空白が入っている。**
  台帳 `inject42.py` の初版は3種とも「無効な注入」になった。複数行にまたがる長い
  文字列を注入元にすると、空行の `'    '` で必ず一致しない。
  **アンカーは空行をまたがない短い断片にすること。**

* **知見24: テストが既定値のまま動いていると、検査したつもりの経路を通らない。**
  fixture が `zoom_scale` を設定しなければ 0.48 で動く。座標を使うテストは、
  **倍率とオフセットを明示すること。** 破壊試験が無ければ気づけなかった。

* **知見25: 文書を機械で書き換えるときは、改行の種類を先に測る。**
  git は CRLF で取り出す。LF 前提で書いた置換は、**複数行にまたがった瞬間に必ず外れる**。
  読み込みで `\r\n` → `\n` に正規化し、書き戻しで元へ戻すこと。

---

## 📌 Task 45 — 水色マークを検出して OCR 範囲にする（2026-08-27 夜）

| 項目 | 値 |
|---|---|
| Jules セッション | `12124659471175689821` |
| 所要 | 21:10 発注 → 21:20 Completed（**11分**） |
| マージ | `2e4299f`（`ae4ab53` + `37091cb`） |
| テスト | 240 → **252 passed**（マージ後の master 本体でも確認・終了状態 0） |
| 変更ファイル | 2件（`custom_gui/mark_detector.py` / `tests/test_mark_detector.py`・どちらも新規） |

**背景**：承認者が誌面に手描きした水色マークを、色だけで機械的に拾う。
Gemini などの LLM を使う案が出たが、指揮官が実測して**色だけで完全に分離できる**ことを
示し、承認者のご判断で色検出を採用した（2026-08-27・案ア）。

**採用しなかった理由（LLM）**：決定論的でないため、この箱の関門（DoD を数値で書き、
欠陥再注入で検査が本当に見分けるか測る）が成立しない。加えて bounding box の座標誤差が
範囲OCR では行の欠落に直結し、課金が発生し、オフラインで動かなくなる。

### 実物3枚での照合（指揮官の独立実測と一致）

| 画像 | 囲み | 傍線 |
|---|---|---|
| `..._000007` | 1（1009×307 の目次枠） | **9**（幅 51-52px に拡張済み） |
| `..._000018` | 1（342×613） | 0 |
| `..._000028` | 2（536×204 / 448×308） | 0 |

### 指揮官が塞いだ穴（`37091cb`）

破壊試験 `loose_threshold`（彩度の下限 120 → 40）が**検出できなかった**。測ると、
case 4 が使う題字インクは HSV(90, 67, 88) で、**明度 88 が明度の下限 180 に掛かって
落ちていた**。つまり**彩度の下限は、どのテストにも守られていなかった**。

明るいが彩度の低い水色（S=68, V=220）を弾く `test_11` を追加。先に彩度を緩めて FAIL を
見てから書いた。再測定で4種すべて検出。

## 🧠 獲得した知見（Task 45）

* **知見26: `cv2.imread` は日本語ファイル名で、例外を投げずに `None` を返す。**
  実測（2026-08-27）。素材は3枚とも日本語名のため、そのまま書かれていれば
  「マークが1つも見つからない」という顔で失敗していた。
  **`cv2.imdecode(np.fromfile(path, np.uint8), cv2.IMREAD_COLOR)` を使うこと。**

* **知見27: OpenCV の色相 H は 0-179 である**（一般的な 0-360 の半分）。
  360 のつもりで閾値を書くと必ず外す。

* **知見28: 1つの条件が2つのしきい値で守られているとき、片方は検査されていない。**
  題字インクは彩度も明度も低いため、明度の下限だけで落ちていた。
  **注入で「落ちなかった」ときは、別の条件が代わりに守っていないかを疑う。**
  Task 42 の「既定値のまま経路を通らない」と並ぶ、破壊試験でしか見えない型である。

---

## 📌 Task 46 / 47 — 並行発注（2026-08-27 夜）

**この箱で初めて2本を同時に走らせた。** ファイルが1つも重ならないことを
確かめたうえで発注し（`/発注` §3）、互いの指示書に相手の担当ファイルを
名指しで禁止した。検収は1本ずつ行った。

| | Task 46 | Task 47 |
|---|---|---|
| セッション | `8890893855532601916` | `16632367043364696728` |
| 中身 | 「マーク読取」ボタン | 橙色 -> `MARK_AD` |
| 触ったファイル | `custom_gui/app.py` | `custom_gui/mark_detector.py` |
| 所要 | 21:56 -> 22:09（13分） | 21:56 -> 22:04（**8分**） |
| マージ | `dd94adc` | `8e91206` |
| 破壊試験 | 4種すべて検出 | 4種すべて検出 |

**テスト 252 -> 270 passed**（2本を統合したあとの master 本体で確認・終了状態 0）。

### 実物での確認

Task 47：52枚に対し**広告9ページを完全一致で判定**（余分0・取りこぼし0）。
`000048` は広告かつ OCR 範囲ありで、両立することを確認。

Task 46：`p000007` を開いて押すと **0 -> 10個（囲み1・傍線9）**。
もう一度押しても **10個のまま**（重複しない）。

### 今回は穴が1つも無かった

Task 42・45 と違い、**指揮官がテストを足す必要がなかった**。
Task 47 の指示書に、Task 45 で踏んだ型への対策を**先回りで**書いたためである。

    「彩度と明度は、それぞれ『その境目だけが弾く標本』で守ること。
      case 7 と case 8 を1本にまとめてはならない」

実際に `loose_sat` と `loose_val` は別々のテストを落とした。

## 🧠 獲得した知見（Task 46 / 47）

* **知見29: 踏んだ型は、次の指示書に書けば繰り返さない。**
  Task 42・45 では検収で穴を見つけて指揮官が塞いだ。Task 47 では同じ型を
  **発注前に**指示書へ書き、穴はゼロだった。
  **日報に書くだけでは繰り返す。指示書に書くと止まる。**

* **知見30: 並行発注はファイル単位で判定する。**
  フォルダ単位ではない。46 は `app.py`、47 は `mark_detector.py` で、
  47 が足した関数を 46 が import する関係だが、46 は master にある版を
  使うため衝突しない。**「読む」と「書く」を分けて数えること。**

* **知見31: `jules remote list` は3回に1回ほど空を返す。**
  発注直後の照会が何も返さず、2回目で両セッションが見えた（実測）。
  **空だったことを「無い」と読まない。** 生の一覧を出して確かめる。

---

## 📌 Task 48 / 49 — マーク読取の完成と、テストの根を断つ（2026-08-28 朝）

| | Task 48 | Task 49 |
|---|---|---|
| セッション | `4861839991404638421` | `10410866095938571546` |
| 所要 | 19分 | **54分**（これまでで最長） |
| マージ | `d7eae1c` | `66d14e9` |
| 破壊試験 | 3種検出・1種は無害 | **4種すべて検出** |

**テスト 275 -> 279 passed**（マージ後の master 本体で確認・終了状態 0）。

### Task 48 — 「マーク読取」で【広告】も記録する

承認者のご判断・案イ。ボタンは増やさず、1回押せば矩形と【広告】の両方。

**設計の核心**は、橙の処理を「水色0件なら return」より**前**に置くことだった。
実測で広告9ページ中8ページが水色を持たないため、後ろに置くと機能が必要な
ページで何も起きない。破壊試験の1本目でこれを見張っている。

実ページ3枚で確認:

    p000002 橙のみ   -> mark=【広告】 矩形0  txt に記録
    p000048 橙+水色  -> mark=【広告】 矩形3  txt に記録
    p000018 水色のみ -> mark=None     矩形1  記録なし

### Task 49 — 検収で見つけた、Task 46 から入っていた2つの不具合

**① 画面に出ていなかった。** `_on_marks_to_rects_click` が `_update_status()` を
呼ばないため、矩形は作られるのに表示が更新されない。実測:

    押す前   ... | Last: None
    内部の値 マークから 0 個の矩形を作りました（囲み 0・傍線 0）
    押した後 ... | Last: None

**② テストが検査対象を差し替えていた。** `get_app()` が `_update_status` /
`_update_selections_ui` / `_persist_work_state` の**3つを空の関数に**していた。
ハンドラの4つの仕事のうち3つが、一度も検査されていなかった。

「UI 例外を防ぐため」という注釈は事実に反した。3つとも本物のまま動く
（`page` に MagicMock、`status_text` に本物の `ft.Text` を与えるだけ）。

**破壊試験の4本目が決定的だった。** 3つのモックを戻すと、
case 14/15/16/17 の**4本ちょうど**が落ちた。モックが何を隠していたかの証拠である。

## 🧠 獲得した知見（Task 48 / 49）

* **知見32: テストがモックしているものは、検査されていない。**
  この箱の「無効なテスト」は通算12件と数えていたが、いずれも
  「ハンドラを迂回した」型だった。今回のは**ハンドラは通るが、その先の
  3つを空にしていた**型である。**迂回だけでなく、差し替えも数えること。**

* **知見33: 内部の値を検査しても、画面に出るとは限らない。**
  `latest_region_info` を検査していたが、そこから `status_text` へ渡す
  `_update_status()` が呼ばれていなかった。
  **利用者が見る値を検査すること。** 変数ではなく画面である。

* **知見34: 「無害な注入」と「見張れていない」は別である。**
  Task 48 の `skip_persist` を「無害」と判定したのは正しかったが、
  そもそも `_persist_work_state` はモックされており、**どんな永続化の欠陥も
  検出できない状態**だった。判定が正しくても、理由の申告が不完全だった。
  **注入が落ちなかったときは、無害かどうかの前に『そもそも見張っているか』を問う。**

* **知見35: 承認者への選択肢は、実測を添えると1往復で決まる。**
  Task 48 のマージで利害を問われたとき、実ページ3枚で動かして表を出した。
  「動くが見えない」という事実が、案アの寿命最短という判断を支えた。

---

## 📌 OCR 精度の確認と Task 50（2026-08-28 昼）

### 52枚の OCR を実行した

| 項目 | 値 |
|---|---|
| 所要 | **9分36秒**（52枚・モデル読込1回・CPU） |
| 出力 | `.txt` / `.xml` / `.json` 各52＝156ファイル（`jpg\_ocr結果\`） |
| 課金 | ゼロ（ONNX 4本・計157MB でローカル完結） |

**本文の精度は良好。** 旧字（經・國・會・來・寫）も拾い、縦書きの読み順も正しい。
弱いのは2点だけだった。**デザイン文字**（題字「國際寫眞新聞」→「聞新貢需際園」）と、
**横書きの左右反転**である。

範囲抽出（`jpg\_ocr結果\_範囲抽出.txt`）はアプリと同じ経路を通した
（`parse_ocr_json` -> `detect_marks` -> `filter_lines_by_region` -> `assemble_text`）。
118範囲から 7,040字。`p000007` は傍線9本が見出しを1行ずつ、囲みが目次38行を正確に抜いた。

### Task 50 — 横書きの範囲に印を付ける

| 項目 | 値 |
|---|---|
| セッション | `8927653653520181933` |
| 所要 | 19分 |
| マージ | `39db6f8` |
| テスト | 279 -> **293 passed** |
| 破壊試験 | 4種すべて検出 |

実データ照合: 印が付く範囲 **39個 / 25ページ / 横書き80行**。

### 契約違反が1件あった

Jules が許可外の `tests/test_save_direct.py` を書き換えていた
（`is_vertical` を False -> True・3か所）。AGENTS.md §5 が禁じる既存テストの改変である。

**ただし衝突自体は本物だった。** `test_f_panel_breaks` はラベルを厳密比較しており、
戻すと `'Region 1 [横書き? 3] [改行 2]:' != 'Region 1 [改行 2]:'` で落ちた（1件のみ）。

変更を一度戻し、指揮官が AGENTS.md §2 の例外として入れ直した（`fix(指揮官):`）。
判断の根拠は、既存テスト38行すべてが `is_vertical: False` で `True` が0行という、
最初の fixture を写した慣習だったこと。本資料は縦書きが 90.7% である。

## 🧠 獲得した知見（Task 50）

* **知見36: 推定を設計する前に、データが答えを持っていないか探す。**
  行の向きを外接矩形の縦横比で判定する案を出したが、`parse_ocr_json` の出力に
  **`is_vertical` が最初から入っていた**。2,235行で照合すると 97.76% 一致し、
  **不一致50件は全て縦横比側の誤り**だった（`(一)` `100` など1-3字の断片）。
  **測ってから発注したので、誤った契約を出さずに済んだ。**

* **知見37: 表示文字列を変える発注の前に、それを厳密比較している既存テストを数える。**
  ラベルに印を足す発注で、既存の1件と衝突した。`grep -rn "改行 " tests/` を
  先に打てば3件見つかり、事前に手当てできた。**指示書を書く前の1回の grep で防げた。**

* **知見38: 作業ツリーには git 管理外のフォルダが無い。**
  照合スクリプトが 0件 を返し、一瞬「実装が壊れている」と読みかけた。原因は
  `jpg/` が `.gitignore` にあり worktree に存在しないことだった。
  **実データでの照合は、絶対パスで正のリポジトリを指すこと。**
